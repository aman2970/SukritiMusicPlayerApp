<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sukriti" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/light_pink</item>
    </style>

    <style name="Theme.Sukriti" parent="Base.Theme.Sukriti" />

    <style name="CustomSeekBar" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressDrawable">@drawable/seekbar_bg</item>
        <item name="android:minHeight">2dp</item> <!-- Set the height of the line -->
        <item name="android:thumb">@drawable/thumb_bg</item>
    </style>
</resources>